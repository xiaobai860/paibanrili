# 2026-08-22

## Glance 小组件版本评审（架构评审 / 部分工作流）
- 项目已用 Jetpack Glance 实现 3 个小组件：ScheduleGlanceWidget(2x1 打卡)、CalendarGlanceWidget(3x3)、Calendar3x4GlanceWidget(3x4 农历)，均 GlanceAppWidget + GlanceAppWidgetReceiver。
- SDK：compileSdk=37, minSdk=34, targetSdk=36。minSdk 34 即 Android 14，无需 RemoteViews 兜底。
- 当前 Glance 版本 = 1.1.1（1.1.0 的 bugfix 补丁，生产推荐线）。
- 结论：留在 1.1.1（生产稳妥）；1.2.0-rc01 仅 RC，待转正式再升，升级时无需改 SDK（minSdk34/compileSdk37 已满足其 23/37 要求）。
- 硬约束符合性：交互走 ActionCallback(✓ 非高频)、provideGlance 仅读尺寸内容里读 prefs(✓)、minSdk34 免 RemoteViews(✓)。
- 唯一偏离：三个 appwidget-provider XML 均设 updatePeriodMillis="1800000"(30min 系统下限)，与「别依赖 updatePeriodMillis」冲突。实际刷新已由 ViewModel 事件驱动，该兜底冗余且 Doze 下不可靠。建议：置 updatePeriodMillis=0，必要时用 WorkManager 周期刷新(15min 下限)兜底。
- 涉及文件：schedule_widget_info.xml / calendar_widget_info.xml / calendar_3x4_widget_info.xml（res/xml）。

## 整改方案 A 落地 + 构建安装（已实现）
- 三个 res/xml/*_widget_info.xml 的 android:updatePeriodMillis 由 "1800000" 改为 "0"。
- 新增依赖：libs.versions.toml 加 workmanager=2.9.1 + workmanager-runtime 库；build.gradle.kts 加 implementation(libs.workmanager.runtime)。
- 新增 app/src/main/java/com/schedulecalendar/app/widget/WidgetRefreshWorker.kt：CoroutineWorker，枚举 ScheduleGlanceWidget/CalendarGlanceWidget/Calendar3x4GlanceWidget 三类 glanceId 触发 .update() 重渲染（仅兜底、不重新拉数据，重活仍由 ViewModel 事件驱动完成）。
- ScheduleApp.onCreate() 用 WorkManager.getInstance.enqueueUniquePeriodicWork("widget_periodic_refresh", UPDATE, PeriodicWorkRequestBuilder<WidgetRefreshWorker>(15min)) 接入；幂等，开机/进程重启后由 WorkManager 自动恢复。
- proguard-rules.pro 已保留 androidx.work.** 与 widget 包全部类，release minify 正常（无需额外 keep）。
- 构建：release 签名已在 local.properties 配置；用 Windows 风格 JAVA_HOME=C:\Program Files\Android\Android Studio\jbr 跑 gradlew.bat :app:assembleRelease（Git Bash 下 JAVA_HOME 不能用 /c/... 形式，否则 gradlew.bat 报 invalid directory）。产物 app/build/outputs/apk/release/app-release.apk。
- 安装：adb 在 C:\Users\xx\AppData\Local\Android\Sdk\platform-tools\adb.exe；设备序列 3B15A901FM600000(device)；`adb install -r app-release.apk` 成功。dumpsys jobscheduler 确认 widget_periodic_refresh 周期任务已调度到 SystemJobService。
- 环境命令范式：cd /e/Android/android_native && JAVA_HOME='C:\Program Files\Android\Android Studio\jbr' ./gradlew.bat :app:assembleRelease

## 应用图标重设计（已实现）
- 为「排班日历」重设计自适应应用图标，保留日历主意象：白卡 + 紫色月份条 + 琥珀「今日」+ 青色「班次条」。
- 已替换项目 drawable：ic_app_schedule_background.xml、ic_app_schedule_foreground.xml、ic_app_schedule_mono.xml，均通过 XML 语法校验。
- 新增预览资源目录 app_icon_preview/：包含 full.svg、mono.svg、icon_1024.png、icon_512.png、preview.html。
- minSdk=26（Android 8.0），自适应图标 mipmap-anydpi-v26/ic_launcher.xml 已引用上述三层，无需额外生成 mipmap PNG。

## 2x1 打卡按钮内边距对称化（已实现）
- 需求：按钮内部「上/下班卡」文字左内边距要与右侧一样（之前左贴边 0、右 2dp），且保留时间文字与按钮的距离。
- 坑：Glance 的 Box 外层 `.padding(start)` 与内层 `.padding(start)` 会叠加（背景框把外边距算进框内），导致左=8dp 右=2dp 不对称。修法：外层 start 归 0，内层 start=2dp、end=2dp，左右对称。时间文字 defaultWeight() 保留（与按钮 gap=0，用户认可）。
- 验证范式：装后用 `adb shell uiautomator dump /sdcard/widget_dump.xml` + pull + Python 解析 parent map，量 box 与 text 的 bounds 差值，直接计算内部左右内边距像素。设备密度 density≈3(xxxhdpi)，2dp=6px。
- 字号上限结论：时间串 "HH:MM – HH:MM"(13字符) 当前 12.sp 实测占满整行剩余宽度(~273px≈91dp)，12.sp 即天花板；13.sp+ 裁切末尾。缩短格式(去空格 "08:30–20:30")可放到 ~14.sp。
- 方案A已落地(用户确认)：ScheduleGlanceWidget 第二行时间 Text 由 "$displayStart – $displayEnd"(含空格) 改为 "$displayStart–$displayEnd"(去空格) + fontSize 12→14.sp；实测 "08:30–20:30" 14.sp 宽 273px=91dp 正好填满可用宽、右缘贴按钮(gap=0)、未裁切、未重叠。时间左对齐/按钮右对齐/中间弹性留白布局不变(时间 defaultWeight 顶满、按钮 wrap_content 居右)。布局意图：距离无所谓，只要时间不裁切。

## 2x1 打卡按钮高度最大化（已实现）
- 需求：第一行/第三行位置不变，打卡按钮高度做到最大(仅变高、宽度/位置不变)，不遮挡上/下行。
- 机制：原三行均 defaultWeight() 等分(各 1/3)，按钮被压在中间 1/3。改成"上下两行 wrap content + 中间行独吞剩余 + 按钮 fillMaxHeight"：第一行 Row 去 defaultWeight()(贴顶,占内容高)、第三行 Box 去 defaultWeight()(贴底,占内容高)、第二行 Row 保留 defaultWeight()(吃满"第一行底→第三行顶"全部剩余空间)、打卡按钮 Box 加 .fillMaxHeight()(撑满第二行高)。
- 实测(dump5)：按钮 box h 62px→120px(≈20.7dp→40dp,翻倍)；宽度 111px(37dp)不变；footer 顶边(y=395)=按钮底边(y=395)紧贴不重叠；第一行仍在顶部未被触碰。密度≈3,2dp=6px。
- 注意：改后第一行会从"1/3区居中"变为"贴顶"、第三行贴底——仍固定在顶/底，这是让按钮最大化且不遮挡的唯一方式。

## 2x1 加手动刷新按钮 + 3x3/3x4 图标去灰底（已实现）
- 评估结论：2x1 建议加刷新按钮(交互一致性 + 配置变更兜底 + 成本极低)；2x1 本身刷新频率高(每次打卡都刷)，刷新按钮是"保险+一致性"非强需求。
- 2x1 新增 RefreshScheduleWidgetAction(ActionCallback)：只刷自身 ScheduleGlanceWidget().update + Toast"刷新成功"(模式同 3x3 的 RefreshWidgetAction)，定义在 ScheduleGlanceWidget.kt(OpenWidgetConfigAction 之后)。
- 2x1 第一行齿轮(⚙)右侧加刷新按钮(↻)：纯 Text(无背景) + Spacer(width=2.dp)，样式与齿轮一致(utc.copy(0.4f) 颜色,12.sp)。
- 3x3/3x4 共用 CalendarWidgetContent：设置(⚙)与刷新(↻)按钮由带灰底方形 Box(.width(refreshSize).height(refreshSize).background(utc.copy(0.08f)).cornerRadius(6)) 改为纯 Text + .padding(horizontal=4,vertical=2).clickable (去灰底，与 2x1 齿轮一致)；同时删除已无用的 refreshSize 变量定义。改一处同时影响 3x3 与 3x4。
- 验证(dump6)：2x1 现含 ↻(bounds 紧邻 ⚙)、三行内容完好未崩；另一组件 ⚙/↻ 均在。去灰底靠代码删除确定性保证，背景色 uiautomator 不可测，需用户肉眼镜像确认(尤其桌面放了 3x3/3x4 时)。
