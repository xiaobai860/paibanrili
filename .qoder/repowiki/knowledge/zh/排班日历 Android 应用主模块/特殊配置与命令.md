Gradle 仓库源优先使用阿里云镜像（maven.aliyun.com），备用华为云与 JitPack；release 签名配置硬编码在 build.gradle.kts 的 signingConfigs 中；Room schema 通过 KSP 参数 room.schemaLocation 自动导出到 app/schemas/；lint 禁用了 HighAppVersionCode、IconLauncherShape 等警告以适配日期格式版本号与默认图标模板。