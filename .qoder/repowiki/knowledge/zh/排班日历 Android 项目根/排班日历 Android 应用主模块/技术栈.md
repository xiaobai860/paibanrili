Kotlin + Compose UI (Material3) + Navigation Compose + Hilt (KSP) + Room (KSP, schema 导出) + DataStore Preferences + Jetpack Glance (Compose-first 小组件) + kotlinx-serialization (类型安全路由) + tyme4j (农历计算) + Gson + Coroutines。编译 SDK 36，最低 API 26，Java 17。