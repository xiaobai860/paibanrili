Room schema 通过 KSP 参数 `room.schemaLocation=$projectDir/schemas` 自动导出到 `app/schemas/com.schedulecalendar.app.data.db.AppDatabase/` 目录，每次版本变更需检查 JSON 文件；release 构建使用硬编码签名配置（storeFile 指向本地 keystore），并启用 proguard-rules.pro 混淆与资源压缩。